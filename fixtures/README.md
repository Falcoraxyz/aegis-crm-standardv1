Fixtures template for golden tests.

Replace these with real deterministic vectors produced by the CLI once implemented.
Files:
- vendor_priv.hex, vendor_pub.hex
- user_priv.hex, user_pub.hex
- license_payload.json
- license.cert (CBOR bytes or base64)
- pop_nonce.hex, pop_sig.hex
